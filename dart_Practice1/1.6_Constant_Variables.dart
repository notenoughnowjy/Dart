//dart에서 const는 compile-time constant를 만들어준다.
// compile-time constant : 프로그램이 컴파일되는 동안 값을 미리 알 수 있는 상수
// final이랑 동일하게 작동, 값을 수정할 수 없다.
// 앱 스토어에 올리기 전에 값을 알고 있는 값
void main() {
  const API = 1234;
  const api = 'hello';
  const AP = 1.2;
}
